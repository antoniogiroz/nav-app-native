import { StyleSheet, Text, View, ViewProps } from 'react-native';

export function FeedScreen(props: ViewProps) {
  return <View style={styles.container}>
    <Text>Feed Screen</Text>
  </View>;
}

const styles = StyleSheet.create({
  container: {},
});

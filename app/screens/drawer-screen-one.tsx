import { StyleSheet, Text, View, ViewProps } from 'react-native';

export function DrawerScreenOne(props: ViewProps) {
  return <View style={styles.container}>
    <Text>DrawerScreenOne</Text>
  </View>;
}

const styles = StyleSheet.create({
  container: {},
});
